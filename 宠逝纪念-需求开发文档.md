# 宠逝纪念 —— 宠物离世后的数字纪念馆
## 需求开发文档（V1.0）

| 项目名称 | 宠逝纪念（Pet Memorial） |
| --- | --- |
| 文档版本 | V1.2（新增：照片级写实美术素材规格与 AI 生成方案，见 5.7 节；幕 6/幕 7 改为平整草地覆土下葬） |
| 编写日期 | 2026-09-03 |
| 运行终端 | Web 端（桌面浏览器）+ 手机端（移动浏览器），同一套代码响应式适配 |
| 数据存储 | 完全本地（无任何网络请求、无服务器） |
| 目标形态 | 本地 Web 应用（双击 index.html 或本地静态服务器即可运行） |

---

## 1. 项目概述

### 1.1 产品定位
「宠逝纪念」是一款为失去宠物的主人提供**数字化告别仪式**与**永久纪念空间**的本地应用。
用户通过一场完整的"火化 → 装罐 → 入棺 → 下葬 → 立碑"交互仪式与宠物告别，仪式结束后生成一座专属数字墓碑，此后每天可以上香、留言、回顾照片，形成持续的纪念寄托。

### 1.2 核心理念
- **仪式感**：告别不是一次点击，而是一幕一幕亲手完成的仪式，每一步都由用户点击推进，赋予"亲手送别"的心理抚慰。
- **陪伴感**：香火每日延续、思念天数每日累加，让纪念成为日常。
- **私密性**：所有数据（文字、照片）只保存在用户本机，不上传、不联网，杜绝隐私顾虑。

### 1.3 目标用户
刚刚失去宠物、希望以某种仪式完成告别，或希望长期保留一份数字纪念的宠物主人。

---

## 2. 总体流程

```
┌──────────────┐
│  启动页/首页  │──首次使用──►┌──────────────────┐
└──────┬───────┘             │ 第一步：信息填写  │
       │                     │ 主人名/宠物名/生日 │
       │                     │ 离世日/上传照片    │
       │                     └────────┬─────────┘
       │                              ▼
       │                     ┌──────────────────┐
       │                     │ 第二步：告别仪式  │（7 幕动画，按钮推进）
       │                     │ ①照片放大         │
       │                     │ ②火焰升起燃烧     │
       │                     │ ③照片化为灰烬     │
       │                     │ ④扫灰入罐（按钮） │
       │                     │ ⑤罐入棺材（按钮） │
       │                     │ ⑥覆土下葬（按钮） │
       │                     │ ⑦墓碑缓缓升起     │
       │                     └────────┬─────────┘
       │                              ▼
       │  已建馆（非首次）     ┌──────────────────┐
       └────────────────────►│ 第三步：纪念馆首页 │◄─┐
                             │ 墓碑场景+香炉三炷香 │  │ 每天跨日后
                             │ 上香按钮/思念天数   │──┘ 香熄灭，重新上香
                             │ 留言/照片墙/设置    │
                             └───────────────────┘
```

**状态机**：应用只有两种全局状态，持久化在本地：
- `UNBUILT`（未建馆）：进入信息填写页。
- `BUILT`（已建馆）：直接进入纪念馆首页（墓碑场景）。

---

## 3. 功能需求

### 3.1 模块一：信息填写页

#### 3.1.1 页面字段

| 字段 | 类型 | 必填 | 说明 | 校验规则 |
| --- | --- | --- | --- | --- |
| 主人名称 | 文本 | ✅ | 用于碑左侧"×××敬立" | 1~20 字符，去首尾空格 |
| 宠物名字 | 文本 | ✅ | 用于碑文"故 ××× 之墓" | 1~20 字符 |
| 宠物生日 | 日期 | ✅ | 碑右侧生卒年月的"生" | 不得晚于离世日期、不得晚于今天 |
| 离世日期 | 日期 | ✅ | 碑右侧生卒年月的"卒"；思念天数起点 | 不得早于生日、不得晚于今天 |
| 宠物照片 | 图片 | ✅ | 仪式燃烧用图 + 碑上遗照 | 单张；jpg/png/webp；≤10MB；建议近似 3:4 竖版 |
| 宠物性别 | 单选 | ⭕ | 默认"保密"；仅用于展示称谓 | 男 / 女 / 保密 |
| 宠物品种 | 文本 | ⭕ | 纪念馆信息卡展示 | ≤20 字符 |
| 想对它说的话 | 多行文本 | ⭕ | 仪式结束后自动写入第一条留言 | ≤500 字符 |

> **优化说明**：原需求只有"生日"，但碑右要刻"生卒年月"，故必须补充**离世日期**；立碑时间不需要用户填写，**自动取仪式完成当天的日期**。

#### 3.1.2 交互要求
1. 照片上传后即时预览；支持点击重新选择替换。
2. 若浏览器支持，自动将照片压缩至最长边 ≤1600px、质量 0.85 后存储（节省本地空间，加速渲染）。
3. 所有必填项校验通过前，"开始告别仪式"按钮置灰不可点击；点击置灰按钮时逐项高亮报错提示。
4. 填写内容实时暂存到本地（草稿），意外关闭页面后重开可恢复。
5. 页面风格：深色夜空背景 + 微弱星光粒子，柔和哀而不伤的暖色点缀；文案温和，例如页面标题为"送它最后一程"。

### 3.2 模块二：告别仪式（7 幕动画）

仪式为全屏沉浸模式，隐藏一切无关 UI。每幕之间由**屏幕下方居中的一个按钮**衔接（按钮文案随幕变化），最后一幕自动进入纪念馆。仪式期间背景播放低沉舒缓的环境音（可静音，默认静音，由用户点击右下角音频开关开启）。

#### 幕 1：照片放大（时长约 1.5s）
- 初始：用户照片以缩略图形式静置于屏幕中央偏下，四周深色。
- 动画：照片以 `cubic-bezier(0.22, 1, 0.36, 1)` 缓动放大至屏幕正中，占屏幕高度约 60%，边缘柔光。
- 结束后浮现按钮：**「点燃送别的火」**。

#### 幕 2：火焰升起（时长约 3s）
- 照片底部边缘先出现一线橙红火光，随后火焰粒子沿照片左右两侧**自下而上蔓延**（Canvas 粒子系统：每帧生成 30~60 个火焰粒子，颜色在 #FF4500 / #FF8C00 / #FFD700 间随机，初速度向上 1~3px/帧，附带横向随机抖动，生命周期 0.5~1.2s，尺寸随生命衰减）。
- 火焰爬满照片四周后整体火势增强，屏幕有轻微暖色泛光（vignette 变橙）。
- 本幕自动衔接幕 3（无需按钮）。

#### 幕 3：照片化为灰烬（时长约 3s）
- 照片叠加**焦边侵蚀效果**：用一张噪声图作为遮罩（mask），从四周向中心逐步扩大遮罩范围，被遮罩区域先变深褐色（sepia+变暗滤镜）、再碎裂为灰烬粒子飘散。
- 灰烬粒子：灰白色小颗粒，受微弱"风"影响横向漂移，缓慢下落，最终在屏幕底部中央**堆积成一小堆灰**（预先绘制好的灰堆图形随粒子到达逐渐显影）。
- 火焰随之熄灭，屏幕恢复深色，仅余灰堆与几缕青烟。
- 结束后浮现按钮：**「把骨灰收进小罐」**。

#### 幕 4：扫灰入罐（时长约 2s）
- 点击按钮后：一只素色小陶罐从灰堆右侧缓缓移入，灰堆化为粒子流被"扫"入罐口（粒子沿贝塞尔曲线飞向罐口，到达后消失）。
- 灰堆完全消失后，罐盖缓缓合上，罐身轻震一下（scale 弹跳 1.0 → 1.05 → 1.0），完成。
- 结束后浮现按钮：**「放入棺材」**。

#### 幕 5：罐入棺材（时长约 2.5s）
- 画面切换/拉远：屏幕中央出现一口木质棺材（俯侧 45° 视角，盖板敞开斜靠一侧）。
- 小罐飘起，缓缓降入棺内正中，落定时棺内微微扬起少许尘埃粒子。
- 盖板从侧面滑下合拢，伴一声轻响（可选音效），棺材封闭。
- 结束后浮现按钮：**「安葬入土」**。

#### 幕 6：覆土下葬（时长约 3s）
- 背景为**平整草地**（星空草地背景素材见 5.7）。草地上先浮现一处浅浅的坑位轮廓，棺材被缓缓吊放/沉降入坑（y 轴下移 + 轻微摇晃）。
- 随后两侧泥土粒子向坑中倾落，逐层覆盖棺材（覆土过程约 2s）；覆土完成后松土自然沉降、被草皮覆盖抚平，地面**恢复为平整草地**（不保留隆起土堆），仅留一圈新翻草地的浅色痕迹。
- 落土结束后地面扬起一圈浮尘，缓缓归于平静。
- 结束后浮现按钮：**「立碑纪念」**。

#### 幕 7：墓碑升起（时长约 3.5s，仪式终幕）
- 下葬处的平整草地轻微震动（镜头 shake 振幅 2px，持续 0.8s），细碎石屑与草叶弹起。
- 一座石碑从**平整草地**中缓缓升起（y 轴上移 + 淡入，缓动 `ease-out`），最终稳定为完整墓碑场景（布局见 3.2.1）。
- 碑前香炉中**三炷香自动点燃**（首次点燃时间 = 当前时刻，写入本地数据）。
- 屏幕浮现一句话文案（2s 淡入，停留 3s）：
  > 「它没有离开，只是换了一个地方，住在你的思念里。」
- 文案淡出后浮现按钮：**「进入纪念馆」** → 点击后状态置为 `BUILT`，跳转纪念馆首页。

#### 3.2.1 墓碑最终样式（核心视觉规格）

```
                ┌─────────────────────────┐
                │      ╭──────────╮        │   ← 碑顶弧形
                │      │  宠物照片 │        │   ← 碑正中上方：照片，宽约碑宽 40%，
                │      ╰──────────╯        │      3:4 竖版，圆角+细金边框
                │                          │
                │     故 [宠物名字] 之墓     │   ← 碑正中、照片下方：楷体大字，
                │                          │      金色/深灰刻字效果
                │  [主人名]   [生年]—[卒年]  │   ← 碑左（观众视角左）：竖排小字
                │   敬立       年月日        │      "××× 敬立" + 立碑日期
                │  立碑日期                 │   ← 碑右：竖排小字 生卒年月
                └─────────────────────────┘
                     ▁▁▁▁ 平整草地 ▁▁▁▁
                        ┌──────┐
                        │ 香炉 │ ≋ 三炷香（持续燃烧，烟丝上缭）
                        └──────┘
```

**碑文内容规则**：
- 正中主碑文：`故 {宠物名字} 之墓`（字体：楷体/衬线体，字号最大）。
- 碑右侧（竖排）：`生于 {生日 YYYY年MM月DD日}　卒于 {离世日期 YYYY年MM月DD日}`。
- 碑左侧（竖排）：`{主人名称} 敬立`　`{立碑日期 YYYY年MM月DD日}`（立碑日期 = 完成仪式当天，自动记录，之后不再变化）。
- 照片：用户上传的宠物照，尺寸缩小置于碑正中上方，带细边框，微微泛光。

> 注：传统碑制以"右为上"，观者视角右侧刻生卒、左侧刻敬立人，与需求一致；实现时以**观众视角**为准。

#### 3.2.2 仪式通用技术要求
- 火焰、灰烬、泥土、烟尘全部采用 **Canvas 2D 粒子系统**实现，单场景同屏粒子上限 800，超出即回收复用（对象池），保证低端设备 ≥30fps。
- 每幕动画支持"跳过"：连按两次屏幕右上角（或按 Esc）出现"跳过仪式"确认弹窗，确认后直接生成最终墓碑场景并进入纪念馆（数据完整一致，仅省略动画）。
- 仪式中途刷新/关闭页面：下次打开时从**当前幕重新开始**（本地记录 `ceremonyStep` 进度），信息填写数据不丢失。
- 动画全部结束后才允许交互，按钮出现带 0.5s 淡入上浮动画。

### 3.3 模块三：上香系统（每日香火逻辑）

#### 3.3.1 燃烧表现
- 香炉置于墓碑正前方，插**三炷香**：香体红色，顶端有橙红燃点微光（呼吸闪烁），上方青烟粒子持续袅袅上升（每炷香独立烟丝，粒子生命周期 2~4s，向上漂移并横向摆动）。
- 页面打开期间香火**持续燃烧**，燃点微光 + 烟雾不间断。

#### 3.3.2 熄灭规则（每日 23:59:59）
本地数据结构记录 `lastLightTime`（上次点燃时间戳）。判定"香已熄灭"的条件（满足其一）：

1. **实时判定**：应用开着时到达当天 23:59:59 → 立即播放熄灭动画（燃点光淡出、最后一缕烟散尽、香顶端变为灰白色），并提示文案：
   > 「今日香火已尽，明日记得再来上香。」
2. **跨日判定**：`lastLightTime` 的日期 ≠ 今天的日期（本地时区自然日）→ 视为香已熄灭。适用于关闭后重开、休眠唤醒、页面重新聚焦（`visibilitychange`）等场景。
3. 熄灭状态下三炷香显示为"燃尽态"（灰白香头、无烟无光）。

#### 3.3.3 上香按钮
- 位置：纪念馆首页香炉正下方悬浮按钮 **「🔥 上香」**。
- 仅在熄灭状态下可点击；燃烧状态下按钮变为不可点的灰色状态，文案「香火燃烧中」。
- 点击后动画：三炷香自顶端依次亮起燃点（间隔 0.2s），烟雾恢复，按钮切换为燃烧态。同时：
  - 更新 `lastLightTime = now`；
  - `totalLightDays`（累计上香天数）+1（同一自然日内重复点击不累加）；
  - 若昨天也上过香，`continuousLightDays`（连续上香天数）+1，否则重置为 1；
  - 弹出轻提示：「已为 {宠物名字} 点上今日香火 🕯 连续第 N 天」。

#### 3.3.4 边界情况
- 修改系统时间/时区：一律以**设备当前本地时间**为准，不做防作弊（纪念意义大于数据严谨性）。
- 23:59:59 熄灭后到 00:00:00 之间的 1 秒内不响应上香（属于"今日已尽"的余韵），00:00:00 后即可上新一天的香。

### 3.4 模块四：纪念馆首页（仪式完成后的常驻页面）

#### 3.4.1 场景布局（自上而下）
1. **顶部信息条**（半透明浮层，可收起）：
   - 左侧：`🐾 {宠物名字}`；
   - 中间：**「已经离开 {N} 天」**（N = 今天 − 离世日期，每日自动更新；离世当天显示"今天"）；
   - 右侧：⚙ 设置入口。
2. **中央主场景**：完整墓碑 + 平整草地 + 香炉三炷香（即幕 7 的最终画面，持续运行香火粒子）。
   - 背景为深色星空（满天星斗 + 平整草地，素材见 5.7），缓慢闪烁/飘落的微光粒子（如萤火/星光），营造静谧氛围。
   - 点击墓碑照片 → 弹出照片大图预览（可左右滑动查看照片墙中的照片）。
3. **香炉下方操作区**：
   - 主按钮：**「上香」**（逻辑见 3.3）；
   - 副文案：`已连续上香 {continuousLightDays} 天 · 累计 {totalLightDays} 天`。
4. **底部功能栏**（圆角卡片按钮组）：
   - 💬 **留言寄思**：打开留言抽屉，可写下想对它说的话；留言按时间倒序展示在"思念卷轴"列表中，每条含时间与文字（≤500 字），可删除单条。
   - 🖼 **照片墙**：网格展示已上传的所有照片（首张为仪式用照片），支持追加上传（每张独立存入本地）、点击大图预览、长按/右键删除。
   - 🕯 **点一支蜡烛**（可选彩蛋）：点击后碑前出现一支蜡烛，燃烧 10 分钟后自动熄灭，纯氛围功能。
   - 🎵 **背景音乐**：开关内置的 1~2 首舒缓纯音乐（本地音频文件，默认关闭）。

#### 3.4.2 周年提醒（轻量优化项）
- 当"今天"是宠物生日或离世周年日（月日匹配）时，首页顶部飘落花瓣粒子，并显示横幅：
  > 「今天是 {宠物名字} 的{生日/离开 N 周年纪念日}，它一定也在想你。」

### 3.5 模块五：设置与数据管理

| 功能 | 说明 |
| --- | --- |
| 修改信息 | 可修改主人名、宠物名、生日、离世日期、性别、品种；修改后碑文与首页实时同步（立碑日期不变） |
| 更换碑上照片 | 从照片墙中选择一张设为主照片 |
| 导出备份 | 将全部数据（JSON + 照片文件）打包导出到本地磁盘（.zip 或单个 .json 含 base64），用户可自行留存 |
| 导入恢复 | 选择备份文件恢复全部数据（覆盖前需二次确认） |
| 重建纪念馆 | 清空全部数据重新开始（需输入宠物名字确认，防误触） |
| 关于/隐私说明 | 明确告知：所有数据仅保存在本机浏览器中，清除浏览器数据会导致纪念馆丢失，建议定期导出备份 |

---

## 4. 非功能需求

| 类别 | 要求 |
| --- | --- |
| 隐私 | 零网络请求：不引入任何 CDN 资源、统计脚本、在线字体；所有 JS/CSS/图片/音频打包在本地目录。**美术素材（陶罐/棺材/背景等）由 AI 在开发阶段离线生成，产物随包本地化，运行期不发生任何联网请求，不违反本原则（详见 5.7）** |
| 性能 | 首页冷启动 ≤2s；粒子场景在 4 年前中端设备（如 8GB 内存的办公本）稳定 ≥30fps；同屏粒子上限 800 |
| 兼容 | Web 端：Chrome / Edge ≥ 100，Firefox ≥ 100；手机端：iOS Safari ≥ 15、Android Chrome ≥ 100、微信内置浏览器。同一套代码响应式适配桌面横屏与手机竖屏，详细规格见 5.6 节 |
| 可靠性 | 任意时刻关闭页面，重开后数据与仪式进度不丢失；本地写入采用"先写临时键再替换"策略防写坏 |
| 存储上限 | 照片统一压缩后存储；单张照片存储 ≤1.5MB；总占用给出可视化提示（设置页显示已用空间） |
| 可访问性 | 所有按钮可键盘聚焦与回车触发；动画遵循系统"减少动态效果"偏好（开启时粒子减半、时长缩短） |
| 情感化文案 | 全站禁用冷冰冰的系统腔（如"错误""失败"），统一使用温和措辞（如"这里好像出了点小问题"） |

---

## 5. 技术方案

### 5.1 总体选型

| 项 | 选型 | 理由 |
| --- | --- | --- |
| 形态 | 纯前端本地 Web 应用（无构建也可运行） | 双击即开、跨平台、无需安装；未来可用 Electron/Tauri 一键打包为桌面 exe |
| 结构 | 原生 HTML + CSS + JavaScript（ES Module），零框架 | 项目规模适中，避免构建链；若开发者熟悉 Vue3 亦可选用（文档按原生编写，切换不影响需求） |
| 动画 | Canvas 2D 粒子系统（火焰/灰烬/烟/土）+ CSS transform/opacity（照片缩放、碑升起、UI 过渡） | Canvas 负责复杂粒子，CSS 负责简单位移，兼顾效果与性能 |
| 持久化 | localStorage（结构化数据）+ IndexedDB（照片二进制 Blob） | localStorage 简单可靠存 JSON；照片体积大必须走 IndexedDB，避免 5MB 配额爆掉 |
| 日期处理 | 原生 `Date` + 自写工具函数（跨日判断、周年判断、天数差） | 无需引入 dayjs 等库，逻辑简单 |

### 5.2 目录结构

```
萌宠数字纪念/
├── index.html              # 单页入口（按状态渲染不同"页面"）
├── css/
│   ├── base.css            # 全局变量、深色主题、字体
│   ├── pages.css           # 填写页/首页/设置抽屉样式
│   └── ceremony.css        # 仪式各幕 UI（按钮、文案）样式
├── js/
│   ├── main.js             # 入口：路由状态机（UNBUILT / BUILT）
│   ├── store.js            # 本地存储封装（localStorage + IndexedDB）
│   ├── pages/
│   │   ├── form.js         # 信息填写页
│   │   ├── memorial.js     # 纪念馆首页（含上香系统）
│   │   └── settings.js     # 设置与数据管理
│   ├── ceremony/
│   │   ├── director.js     # 仪式总导演：幕切换、进度保存、跳过
│   │   ├── acts.js         # 幕 1~7 各幕编排（时间轴）
│   │   └── scene.js        # 墓碑场景绘制（碑/土堆/香炉，首页复用）
│   ├── fx/
│   │   ├── particle.js     # 粒子基类 + 对象池
│   │   ├── fire.js         # 火焰发射器
│   │   ├── ash.js          # 灰烬/烟尘发射器
│   │   ├── soil.js         # 泥土/浮尘发射器
│   │   └── incense.js      # 三炷香：燃点微光 + 烟丝 + 熄灭/点燃动画
│   └── utils/
│       ├── date.js         # 跨日判断、天数差、周年判断、中文日期格式化
│       └── image.js        # 照片读取、压缩、格式校验
├── assets/
│   ├── audio/              # 环境音、可选背景音乐（本地文件）
│   └── img/                # 照片级写实素材：陶罐(罐身/罐盖)、棺材(棺体/棺盖)、香炉、星空草地背景等，分层透明 WebP（详见 5.7）
└── docs/
    └── 宠逝纪念-需求开发文档.md
```

### 5.3 本地数据模型

**localStorage 键：`pet_memorial_data`（JSON 字符串）**

```json
{
  "version": 1,
  "state": "BUILT",                  // UNBUILT | CEREMONY | BUILT
  "ceremonyStep": 7,                 // 仪式进度（0~7），BUILT 后固定为 7
  "pet": {
    "ownerName": "小凡",
    "petName": "豆豆",
    "birthday": "2012-05-20",
    "deathDate": "2026-08-30",
    "gender": "female",              // male | female | secret
    "breed": "中华田园犬"
  },
  "stele": {
    "erectedAt": "2026-09-03"        // 立碑日期，仪式完成当天写入，之后不变
  },
  "incense": {
    "lastLightTime": 1788479999000,  // 上次点燃时间戳（ms）
    "totalLightDays": 12,            // 累计上香天数
    "continuousLightDays": 5         // 连续上香天数
  },
  "messages": [                      // 留言（倒序渲染）
    { "id": "m_1725350400", "text": "豆豆，家里一切都好。", "createdAt": 1788479999000 }
  ],
  "mainPhotoId": "p_1",              // 碑上主照片 id
  "settings": { "bgmEnabled": false, "reducedMotion": false }
}
```

**IndexedDB：库 `pet_memorial_db`，仓库 `photos`**

| 字段 | 说明 |
| --- | --- |
| id | `p_{timestamp}` |
| blob | 压缩后的照片 Blob |
| createdAt | 上传时间 |
| isMain | 冗余标记（与 mainPhotoId 保持一致，便于照片墙排序置顶） |

**草稿键：`pet_memorial_draft`** —— 信息填写页实时暂存，建馆成功后删除。

### 5.4 关键逻辑伪代码

**香火跨日判定（应用启动 / visibilitychange / 每分钟定时器）：**

```js
function isIncenseLit(data) {
  const t = data.incense.lastLightTime;
  if (!t) return false;
  return isSameLocalDay(t, Date.now());   // 上次点燃与现在是同一自然日 → 燃烧中
}
// 每日 23:59:59 触发：setTimeout 精确定位到当天 23:59:59.000 → 播放熄灭动画
// 兜底：每 30s 轮询一次 isIncenseLit，防止 setTimeout 因休眠失准
```

**思念天数：**

```js
daysGone = floor((todayMidnight - deathDateMidnight) / 86400000)
// daysGone === 0 → 显示“今天”；否则显示“已经离开 N 天”
```

### 5.5 动画实现要点

| 效果 | 实现方式 |
| --- | --- |
| 照片放大 | CSS `transform: scale()` + `transition`，GPU 合成，零卡顿 |
| 火焰 | Canvas 粒子：底部发射，向上初速 + 湍流抖动，色带 黄→橙→红 随生命衰减，`globalCompositeOperation = 'lighter'` 叠加发光 |
| 焦边侵蚀 | 离屏 Canvas：照片像素 × 噪声遮罩（随时间扩张），遮罩边缘输出褐色渐变，内部透明 |
| 灰烬飘落 | 灰白粒子 + 重力 0.02 + 横向风噪声（sin 叠加），落到底部 y 阈值后转为静态灰堆贴图显影 |
| 粒子扫入罐/棺 | 粒子目标点沿二次贝塞尔曲线插值运动，到达即回收 |
| 墓碑升起 | Canvas 绘制碑体（或 DOM 元素）`translateY` 从平整草地内上移 + 镜头 shake + 石屑/草叶粒子 |
| 香燃点 | 径向渐变圆点，透明度 `0.7 + 0.3*sin(t*3 + 随机相位)` 呼吸闪烁 |
| 青烟 | 每炷香一条独立粒子链：半透明白色小圆，上升 + 正弦横摆 + 半径渐大 + 透明度渐隐 |

### 5.6 Web 端与手机端响应式适配方案

本应用要求**同一套代码**同时兼容 Web 端（桌面浏览器）与手机端（移动浏览器），不单独开发两个版本。

#### 5.6.1 断点与布局策略

| 断点 | 判定 | 布局策略 |
| --- | --- | --- |
| 手机端（竖屏） | 视口宽 ≤ 768px | 单列纵向流式布局，所有区块全屏宽 |
| 平板/桌面（横屏） | 视口宽 > 768px | 居中容器（最大宽 1080px），区块横向留白 |

- `<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">` 必须配置；适配 iPhone 刘海屏安全区（`env(safe-area-inset-*)`），底部功能栏不得被 Home 指示条遮挡。
- 全部尺寸使用 `rem / vw / vh / %` 与 CSS 变量，禁止写死 px 布局（1px 边框与粒子参数除外）。
- 屏幕方向旋转（手机横竖屏切换、桌面窗口缩放）时通过 `resize` + `orientationchange` 事件重算 Canvas 尺寸并重排场景，**已播放的仪式幕从当前幕头部重放**，不出现画面错位。

#### 5.6.2 各页面/场景适配要点

| 场景 | Web 端 | 手机端 |
| --- | --- | --- |
| 信息填写页 | 表单居中卡片（宽约 480px），字段单列 | 表单全屏宽；日期字段唤起原生日期选择器（`input[type=date]`）；输入框字号 ≥16px 防止 iOS 自动放大页面 |
| 告别仪式 | 照片放大后约占屏高 60%，按钮屏幕下方居中 | 照片约占屏高 45%~50%（为键盘/手势区留白）；幕间按钮固定底部安全区上方，高度 ≥48px 满足拇指点击 |
| 墓碑场景 | 按视口高度等比缩放（碑高 ≈ 55vh），碑文竖排完整展示 | 碑高 ≈ 50vh，碑文竖排字号随碑宽等比缩小（Canvas 绘制天然等比缩放）；照片与文字保证 ≥ 可读下限（主碑文视觉字号不低于 14px 等效） |
| 纪念馆首页 | 顶部信息条 + 中央场景 + 底部功能栏一屏呈现 | 纵向滚动：信息条吸顶（半透明），场景占约 60vh，底部功能栏改为**固定悬浮底栏**（大按钮，间距 ≥8px） |
| 留言/照片墙抽屉 | 侧边滑出抽屉（宽 400px） | 底部滑出面板（高度 ≤80vh），支持手势下滑关闭 |

#### 5.6.3 交互方式双适配

- 所有 `click` 事件天然兼容触摸，不使用 hover 承载任何**必要**信息（hover 仅作桌面端增强）。
- 照片墙的"长按/右键删除"在手机端为**长按 500ms 弹出操作菜单**，桌面端为右键菜单，两端菜单项一致。
- 仪式"跳过"入口：桌面端 Esc / 双击右上角；手机端为右上角常驻半透明小按钮（避免误触需二次确认弹窗）。
- 手机端禁用双击缩放与长按选中对仪式全屏体验的干扰（仪式模式下 `touch-action: manipulation`、`user-select: none`）。

#### 5.6.4 性能与存储差异处理

| 项 | Web 端 | 手机端 |
| --- | --- | --- |
| 粒子上限 | 800 | 400（生成量减半，视觉密度通过加大单粒子尺寸补偿） |
| 照片压缩 | 最长边 ≤1600px | 最长边 ≤1280px；调用系统相机/相册选图（`accept="image/*"`） |
| 帧率兜底 | 连续 1s <24fps 粒子减半 | 同左，且手机端默认开启"省电模式"检测（`navigator.getBattery` 低电量时粒子再减半，不支持该 API 则忽略） |
| 存储配额 | IndexedDB 配额充足 | iOS Safari 对 IndexedDB 有 7 天未访问清理策略 → 首页常驻温和提示"定期导出备份"，建馆完成时自动引导导出一次 |

### 5.7 美术素材规格与 AI 生成方案

本节定义照片级写实素材的风格、分层、格式、适配与降级规格。素材直接决定仪式（罐/棺/背景）与首页主场景的视觉质感，为本期核心美术基线。

#### 5.7.1 总体风格与生成方式
- **风格**：照片级写实（photorealistic），暗调统一，柔和“哀而不伤”的氛围。
- **生成方式**：全部素材由 AI 文生图在**开发阶段离线生成**，经抠图/压缩后作为本地文件打包进 `assets/img/`；**运行期零联网**，与第 4 章隐私原则一致。
- **版权**：确认所用 AI 生成内容可自由使用、无版权风险；不得引用任何外链图片。

#### 5.7.2 统一视角与光源（一致性硬约束）
- **视角**：所有主体物（陶罐、棺材、香炉、蜡烛等）统一采用 **45° 俯侧三分之四视角**，与棺材透视一致，避免合成时“飘”。
- **光源**：统一 **左上方暖色主光 + 暗调环境光 + 金色点缀高光**；所有素材阴影方向一致（右下投影）。
- **色调**：统一暗调 + 微暖金，背景与各主体色温一致；必要时在合成阶段叠加统一色调/暗角/景深做二次协调。

#### 5.7.3 素材分层清单（关键）
为支持仪式动画（合盖、开盖、下葬），主体物必须**分层出图**，不得用整图：

| 素材 | 分层文件 | 用途 | 透明背景 | 参考尺寸(px) |
| --- | --- | --- | --- | --- |
| 陶罐-罐身 | urn_body.webp | 幕 4 扫灰入罐、罐身弹跳 | 是 | ≥1024 |
| 陶罐-罐盖 | urn_lid.webp | 幕 4 罐盖缓缓合上 | 是 | ≥1024 |
| 棺材-棺体 | coffin_body.webp | 幕 5 开口承罐、幕 6 下葬 | 是 | ≥1536 |
| 棺材-棺盖 | coffin_lid.webp | 幕 5 盖板滑下合拢 | 是 | ≥1536 |
| 香炉 | incense_burner.webp | 幕 7 及首页三炷香承托 | 是 | ≥1024 |
| 星空草地背景 | bg_night_grass.webp | 幕 6/幕 7 及首页主场景背景 | 否（整幅） | ≥1792×1024 |

- **宠物通用标识**：陶罐与棺材表面统一带**跨物种宠物纪念元素**——金色爪印 + 狗/猫/兔/鸟/鱼/龟/仓鼠等多宠物浮雕环带，不局限于猫狗，适配所有宠物。（可选：后续支持按用户选择的宠物类型高亮对应标识，本期为通用环带。）

#### 5.7.4 透明背景与抠图
- 主体物出图时置于**纯中性灰无缝背景**，生成后做背景移除，输出带 alpha 通道的 PNG/WebP。
- 抠图边缘要求：无白边/无灰边残留，边缘羽化 ≤1px，半透明过渡（如棺内织物阴影）自然。

#### 5.7.5 压缩格式与体积预算
- **格式**：主体物统一 **WebP（带 alpha）**；不支持 WebP 的旧环境降级 PNG（`<picture>` 或运行时特性检测）。
- **单张体积预算**：主体物 ≤ 300KB/张，背景 ≤ 500KB/张。
- **总体积预算**：`assets/img/` 合计 ≤ 3MB（含各分辨率版本）。
- **加载策略**：按幕**懒加载**——仅在进入对应幕前预取该幕所需素材，避免首屏一次性加载全部。

#### 5.7.6 高清屏（devicePixelRatio）适配
- 素材按 **2× 基准分辨率**导出，运行时根据 `window.devicePixelRatio` 选择合适版本或等比缩放绘制，保证 Retina/高 DPI 手机上不糊。
- 低端机/低 DPR 设备使用 1× 版本，减少内存与解码开销。
- Canvas 绘制素材时以 `dpr` 缩放画布 backing store，避免拉伸模糊。

#### 5.7.7 加载失败降级
- 每张素材设置加载失败兜底：AI 素材缺失/解码失败时，降级为**代码绘制的简化矢量图形**（Canvas 绘制素色罐/棺/星空草地），保证仪式与首页流程不中断。
- 降级时不报错、不弹系统腔提示（遵循第 4 章情感化文案），静默切换。

---

## 6. 验收标准（关键用例）

| # | 用例 | 预期结果 |
| --- | --- | --- |
| 1 | 首次打开应用 | 进入信息填写页；必填项为空时开始按钮不可用 |
| 2 | 生日晚于离世日期 | 表单报错，无法进入仪式 |
| 3 | 完整走完 7 幕仪式 | 动画按序播放，幕间按钮正确衔接，最终生成墓碑，三炷香点燃 |
| 4 | 碑文核对 | 正中上方为缩小版照片；下方"故 {宠物名} 之墓"；右侧生卒年月；左侧"{主人名} 敬立"+立碑日期（=完成当天） |
| 5 | 仪式中刷新页面 | 重开后从当前幕继续，表单数据完整 |
| 6 | 跳过仪式 | 确认后直接得到与完整仪式一致的最终墓碑与数据 |
| 7 | 建馆后重开应用 | 直接进入纪念馆首页，不出现填写页与仪式 |
| 8 | 当天已上香，23:59:59（应用开着） | 播放熄灭动画并提示；按钮变为可点的「上香」 |
| 9 | 昨日上过香，今日重开应用 | 香为熄灭态；点击上香后 `continuousLightDays` +1 |
| 10 | 昨日上过香，隔一天（前天）没上 | 上香后连续天数重置为 1，累计天数 +1 |
| 11 | 同一天多次点击上香 | 天数不重复累加，仅播放点燃效果 |
| 12 | 上传 20MB 照片 | 拦截并温和提示 ≤10MB；压缩后实际存储 ≤1.5MB |
| 13 | 导出备份 → 清空数据 → 导入备份 | 信息、照片墙、留言、香火统计完整恢复 |
| 14 | 离世周年日打开首页 | 显示纪念横幅 + 花瓣粒子 |
| 15 | 断网环境运行 | 全功能正常（验证零外部依赖） |
| 16 | 手机竖屏（375×667 及 390×844）完整走完仪式 | 各幕画面不裁切、按钮可正常点击（≥48px），碑文在手机屏上清晰可读 |
| 17 | 仪式过程中旋转手机屏幕 / 缩放桌面窗口 | Canvas 重算尺寸，当前幕从头重放，无错位、无拉伸变形 |
| 18 | 手机端照片墙长按照片 | 500ms 后弹出删除菜单，与桌面端右键菜单功能一致 |
| 19 | iPhone 底部安全区 / 刘海屏 | 底部功能栏与仪式按钮不被 Home 指示条、刘海遮挡 |
| 20 | 幕 6/幕 7 在平整草地背景下走完 | 覆土下葬后地面恢复平整草地（无隆起土堆），墓碑从草地中升起，衔接自然 |
| 21 | 检查陶罐/棺材抠图边缘 | 透明背景无白边/灰边残留，叠加到草地上边缘自然 |
| 22 | 幕 4 合盖 / 幕 5 盖板合拢分层动画 | 罐身+罐盖、棺体+棺盖分层素材独立运动，开合正确无穿帮 |
| 23 | 所有主体物视角/光源一致性 | 陶罐、棺材、香炉均为 45° 俯侧视角、同一光源方向，合成无违和 |
| 24 | 高 DPI（devicePixelRatio≥2）手机查看素材 | 陶罐/棺材/背景清晰不模糊 |
| 25 | 人为使素材加载失败（改名/删除文件） | 静默降级为代码绘制图形，仪式与首页流程不中断，无系统腔报错 |
| 26 | 断网 + 素材总体积核查 | assets/img 合计 ≤3MB，全本地加载，无外链请求 |

---

## 7. 开发排期建议（单人开发）

| 阶段 | 内容 | 预估工时 |
| --- | --- | --- |
| M1 基础框架 | 目录搭建、状态机路由、store.js（localStorage+IndexedDB）、日期工具 | 1 天 |
| M2 信息填写页 | 表单、校验、照片压缩预览、草稿暂存 | 1 天 |
| M3 粒子引擎 | particle.js 对象池、fire/ash/soil 发射器、焦边侵蚀遮罩 | 2 天 |
| M4 告别仪式 | director + 7 幕编排、按钮衔接、进度保存、跳过逻辑 | 2~3 天 |
| M4.5 美术素材 | AI 生成罐身/罐盖/棺体/棺盖/香炉/星空草地背景、抠图透明化、WebP 压缩、多分辨率与降级图形（见 5.7） | 1~2 天 |
| M5 墓碑场景 | scene.js 碑体绘制（碑文排版/照片嵌入）、首页复用 | 1~2 天 |
| M6 上香系统 | incense.js、跨日判定、23:59:59 熄灭、上香按钮与统计 | 1 天 |
| M7 纪念馆首页 | 信息条、留言、照片墙、周年提醒、背景音乐、蜡烛彩蛋 | 2 天 |
| M8 设置与备份 | 修改信息、导出/导入、重建、空间占用显示 | 1 天 |
| M9 打磨验收 | 手机端响应式适配（5.6 节全部要点）、减少动态偏好、性能调优、素材集成与降级验证、26 条验收用例回归（Web 端 + 手机端各一轮） | 2~3 天 |
| **合计** | | **约 14~18 个工作日** |

---

## 8. 风险与备选方案

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| localStorage 5MB 配额 | 数据写入失败 | 结构化数据极小（<50KB）无风险；照片一律走 IndexedDB（配额通常为磁盘的百分比，GB 级） |
| 用户清除浏览器数据 | 纪念馆永久丢失 | 首页/设置页常驻温和提醒"定期导出备份"；建馆完成时自动引导导出一次 |
| 低端设备粒子掉帧 | 动画卡顿 | 帧率自适应：连续 1s <24fps 时粒子生成量减半；提供"减少动态效果"开关 |
| 设备休眠导致 23:59:59 定时器失准 | 香未按时熄灭 | 30s 轮询兜底 + visibilitychange 唤醒即校验跨日 |
| Safari 对 IndexedDB Blob 的兼容差异 | 照片读取失败 | 统一经 image.js 封装读写；Safari 下降级为 base64 存储（写入前校验体积） |
| AI 素材风格/视角不统一 | 合成违和、显假 | 统一提示词模板（45°视角+左上暖光+暗调）、同批次生成；合成阶段叠加统一色调/暗角；不一致则重出 |
| 素材体积过大拖慢首屏 | 加载慢、低端机卡顿 | WebP 压缩 + 体积预算（≤3MB）+ 按幕懒加载 + DPR 分档；失败降级代码绘制 |

---

## 9. 未来可扩展方向（V2 展望，不在本期范围）

- 多宠物纪念馆切换（一个应用纪念多只毛孩子）；
- 电子祭品：献花、放食物、烧纸钱等轻量互动动画；
- 纪念日历：以"上香打卡"点亮日历热力图；
- 语音寄语：录制一段话存本地，点击墓碑播放；
- Electron/Tauri 打包为独立桌面应用（数据落盘为本地文件，彻底摆脱浏览器存储风险）；
- AI 追思文案：根据用户输入生成悼词（需联网，默认关闭，与隐私原则冲突时不启用）。

---

> 本文档为 V1.2 需求基线。开发过程中如与原需求冲突，以本文档为准；文档变更需同步更新版本号与变更记录。

## 变更记录

| 版本 | 日期 | 变更内容 |
| --- | --- | --- |
| V1.0 | 2026-09-03 | 首版：完整需求基线 |
| V1.1 | 2026-09-03 | 新增 Web 端与手机端画面兼容需求：补充 5.6 响应式适配方案（断点布局、场景适配、双端交互、性能存储差异）；强化第 4 章兼容条款；验收用例 15 条 → 19 条；M9 工时上调 |
| V1.2 | 2026-09-03 | 新增 5.7《美术素材规格与 AI 生成方案》（照片级写实、罐身/罐盖/棺体/棺盖分层、宠物通用标识、透明背景抠图、统一 45° 视角与光源、WebP 压缩与 ≤3MB 体积预算、devicePixelRatio 适配、加载失败降级）；第 4 章澄清 AI 素材开发期离线生成不违反零网络原则；重新设计幕 6/幕 7 与 3.2.1/3.4.1：平整草地背景下改为覆土下葬、墓碑直接立于草地（取消隆起土堆）；验收用例 19 条 → 26 条；排期新增 M4.5 美术素材、第 8 章补充素材相关风险 |
